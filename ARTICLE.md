# 让SKILL可以指定最适合的模型，进行协同工作

## 前言

最近在思考一个问题：在AI技术高速发展的今天，我们有越来越多优秀的大模型可用——Claude擅长代码和深度推理，Gemini在多模态和实时信息方面表现出色，还有其他各具特色的模型。

那么问题来了：**能否在一个统一的工作流中，根据任务的特点，灵活选择最适合的模型来执行？** 这就是我这次实验的出发点。

---

## 背景：为什么要做这个？

### 传统做法的问题

通常我们使用AI时，要么全程用一个模型，要么频繁地在不同API之间切换代码。这存在几个问题：

1. **资源浪费** - 一个模型处理所有任务，即使它不是最优选择
2. **成本不优** - 可能为不适合的模型付费，比如用Claude处理Gemini擅长的任务
3. **体验不一致** - 频繁切换模型导致工程复杂度高、维护困难
4. **能力被限制** - 无法发挥每个模型的最大潜能

### 启发

我在研究Claude官方的Skill实现标准时，发现了一个有趣的特性：**在Skill定义中可以指定使用的模型**。

这给了我灵感 - 为什么不设计一个框架，让它能够：
- 定义一个总工作流（Main Orchestration Skill）
- 该工作流由多个子技能组成
- 每个子技能可以指定由不同的模型执行
- 框架自动调度和协调

这就像是建立了一个"AI包工头"系统，每个工人（模型）根据自己的特长来接活。

---

## 项目设计

### 核心架构

我设计了一个简单但有意义的验证框架，包含三层：

```
┌─────────────────────────────────────┐
│  Main Process Skill (主流程编排层)  │  
│   - 负责工作流协调                   │
│   - 委派任务给子技能                 │
└──────────────┬──────────────────────┘
               │
        ┌──────┴──────┐
        ▼              ▼
┌───────────────┐  ┌─────────────────┐
│ Drawing Skill │  │  Others (待开发) │
│  (由Claude执行) │  │  (由Gemini执行) │
└───────────────┘  └─────────────────┘
```

### 三个关键问题

在设计时，我想验证的核心问题是：

1. **Skill间能否互相调用？** 
   - Main Process能否委派给Drawing Skill？
   
2. **能否在Skill中指定执行模型？**
   - 按照Claude的标准，理论上是可以的
   
3. **框架实现是否完整？**
   - 如果不完整，我们能否通过修改底层代码来支持？

---

## 实验过程

### 第一步：设计Skill标准结构

我创建了三个核心Skill：

**1. skill-creator** - 技能创建指南
- 定义了Skill应该如何编写
- 规范了格式和标准

**2. main-process** - 主流程技能
```markdown
Workflow:
1. Step 1: 打印"主流程正在执行中..."
2. Step 2: 委派给drawing技能绘制Hello World
```

**3. drawing** - 绘图技能（作为演示子技能）
- 支持多种绘图格式（Mermaid、SVG、ASCII等）
- 可独立执行或被其他Skill调用

### 第二步：验证Skill调用链

执行命令：`执行技能main-process`

**输出结果：**
```
主流程正在执行中...

 _   _      _ _      __        __         _     _ 
| | | | ___| | | ___ \ \      / /__  _ __| | __| |
| |_| |/ _ \ | |/ _ \ \ \ /\ / / _ \| '__| |/ _` |
|  _  |  __/ | | (_) | \ V  V / (_) | |  | | (_| |
|_| |_|\___|_|_|\___/   \_/\_/ \___/|_|  |_|\__,_|

✅ 主流程执行完成！
```

**结论：✅ Skill间调用成功！**

### 第三步：观察

在这个过程中，我观察到：

1. Main Process能够成功委派给Drawing Skill
2. 框架正确地追踪了执行流程
3. 这证实了Claude Skills确实支持嵌套调用

---

## 关键发现

### 发现1：Skill级别的工作流编排可行

我们可以设计更大粒度的组织层级：
- 不是函数调用，而是Skill调用
- 每个Skill可以包含自己的逻辑和资源
- Skill之间可以松耦合地交互

### 发现2：模型指定框架已有基础

Claude的Skill标准已经为指定模型预留了接口。这意味着：
- 技术基础存在
- 剩下的是让框架实现这个标准

### 发现3：开放性给了我们改进空间

如果Claude官方的框架实现不完整，我们有能力：
- 研究底层实现
- 贡献改进方案
- 甚至自己扩展框架

---

## 实验输出

这个项目现在包含：

```
two-sklls/
├── .claude/
│   └── skills/
│       ├── main-process/        # ✅ 已完成
│       │   └── SKILL.md
│       ├── drawing/             # ✅ 已完成  
│       │   └── SKILL.md
│       │   └── references/
│       └── skill-creator/       # ✅ 已完成
│           └── SKILL.md
└── README.md                    # ✅ 已完成
```

### 可复现的实验

任何人都可以：
1. Clone这个项目
2. 执行`执行技能main-process`
3. 观察Skill间的调用过程
4. 基于这个框架添加自己的技能

---

## 下一步：多模型编排

### 短期目标（当前阶段）
- ✅ 验证Skill间可以互相调用 
- 🔄 在Skill中正式指定模型参数
- 🔄 测试从Claude切换到Gemini

### 中期目标
- 实现模型切换时的API自动适配
- 建立模型选择策略（基于成本、速度、精度）
- 添加失败自动降级（模型不可用时自动切换）

### 长期目标
- 支持多个AI平台（OpenAI、Anthropic、Google等）
- 建立"AI工作流市场"，预配置好各行业的最优工作流
- 在真实业务中验证ROI提升

---

## 思考：这意味着什么？

这个小实验其实触及了一个更深层的问题：

**在AI时代，系统设计的重点将从"选择一个最强的模型"转变为"根据场景灵活编排不同模型"**。

就像20年前互联网初期，我们从"使用一个全能的框架"发展到"微服务+多语言+最佳工具选择"一样，AI系统也会演变。

未来的超级应用，可能不是基于单一模型的，而是：
- 用Claude处理复杂推理的业务逻辑
- 用Gemini处理多模态和实时信息
- 用专用小模型处理高频低复杂度任务
- 用自训模型处理专有业务

这样的系统将是：
- 📊 **更经济** - 每个模型只处理它擅长的事
- ⚡ **更快速** - 可用最轻量级适配
- 🎯 **更准确** - 专业模型的专业能力
- 🔧 **更灵活** - 可快速适应模型更新

---

## 邀请

这个项目是开放的实验。我邀请感兴趣的人：

1. **Reviewer** - 审视设计是否合理
2. **Contributor** - 贡献新的Skill或模型适配
3. **User** - 在实际项目中尝试这个框架
4. **Thinker** - 讨论多模型编排的最佳实践

---

## 总结

这不仅仅是一次技术实验，而是对**AI应用未来架构**的一次思考。

> 我们不是在寻找最强的模型，而是在设计能让每个模型发挥所长的系统。

项目地址：`e:\development\two-sklls`

有任何想法或建议，欢迎讨论！

---

**写于 2026年1月19日**

*"在AI的世界里，多样性的力量正在显现。"*
